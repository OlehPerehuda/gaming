import React, { useState } from 'react';

export const Comments: React.FC = () => {
    const [visibleTextarea, setVisibleTextarea] = useState<boolean>(false);

    return (
        <section className="details__comments comments">
           
        </section>
    );
};
